# Lotto Android App
통합 로또 HTML을 Android WebView로 실행하는 프로젝트입니다.

GitHub Actions의 **Build Android APK** workflow가 main 브랜치 push 시 APK를 자동 생성합니다.
빌드 완료 후 Actions 실행 화면의 Artifacts에서 `lotto-app-debug-apk`를 받으면 됩니다.
